public interface Engine {
  
    void hybrid();

    //In interfaces, all methods are "public" by default. no need to type
    //In interfaces, no need to type "abstract" for the methods, bcs methods are "abstract" by default
    //So you must override "hybrid()" method in child classes..
 }